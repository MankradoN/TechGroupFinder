import Layout from "../Layout/Layout"
const Favourites = () => {
  return (
    <Layout showSearchBar={false}>
      {/* Favorites page content from database */}
    </Layout>
  )
}

export default Favourites